services:
  - type: web
    name: signscan
    env: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "10000"
      - key: GOOGLE_CLIENT_ID
        value: ""
